{% set name = "clisops" %}
{% set version = "0.8.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 9d32d935bec6c42ad6d117262231ea42538cca377833f8099b69067d5f7c5322

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python
    - pip
    - pytest-runner
  run:
    - python >=3.6
    - numpy >=1.16
    - xarray >=0.15
    - pandas >=1.0.3
    - cftime >=1.4.1
    - netCDF4 >=1.4
    - poppler >=0.67
    - shapely >=1.6
    - geopandas >=0.7
    - xesmf >=0.6.2
    - dask >=2.6.0
    - bottleneck >=1.3.1
    - pyproj >=2.5
    - roocs-utils >=0.5.0
    - requests >=2.0
    - cf_xarray >=0.5.1
    - pygeos >=0.9

test:
  imports:
    - clisops

about:
  home: https://pypi.org/project/clisops/
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: clisops - climate simulation operations

  description: |
    The clisops package (pronounced "clie-sops") provides a python library for running
    data-reduction operations on xarray data sets or files
    that can be interpreted by xarray. These basic operations (subsetting, averaging and
    regridding) are likely to work where data structures are NetCDF-centric, such as those
    found in ESGF data sets.
  doc_url: https://clisops.readthedocs.io/en/latest/
  dev_url: https://github.com/roocs/clisops

extra:
  recipe-maintainers:
    - ellesmith88
    - Zeitsperre
    - agstephens
    - cehbrecht
